<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Party Freak</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
      <header class="header">
        <div class="inner">
          <div class="nav">
            <div class="nav__left">
              <%= link_to "Party Freak", root_path %>
            </div>
            <% if user_signed_in? %>
              <div class="greeting">
                ようこそ！
                <%= link_to "#{current_user.nickname}さん", "/users/#{current_user.id}", class: :greeting__link %>
              </div>
              <div class="nav__right"> 
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :nav__logout %>
                <% if current_user.admin? %>
                  <%= link_to "New Party", new_party_path, class: :nav__btn %>
                <% end %>
              </div> 
            <% else %>
              <div class="nav__right">
                <%= link_to "ログイン",  new_user_session_path, class: :nav__btn %>
                <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
              </div>
            <% end %>
          </div>
        </div>
      </header>
      <%= yield %>
      <footer class="footer">
        <p class="copyright">Copyright © Party Freak All rights reserved.</p>
      </footer>
  </body>
</html>
